{
  "name": "server-test",
  "version": "0.0.1",
  "description": "Express server testing using Mocha and super test",
  "main": "server.js",
  "scripts": {
    "test": "mocha **/*.test.js",
    "test-watch": "nodemon --exec \"npm test\""
  },
  "keywords": [
    "mocha"
  ],
  "author": "ricardo",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2"
  },
  "devDependencies": {
    "expect": "^1.20.2",
    "mocha": "^3.0.0",
    "rewire": "^2.5.2",
    "supertest": "^2.0.0"
  }
}
